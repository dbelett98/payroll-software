// index.js: Main entry file (sets up React root – free open-source React).
import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementId('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);